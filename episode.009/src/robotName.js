
class RobotName {
    name;

    constructor() {
        this.name = this.getName();
    }

    getName() {
        const 
        const num = Math.floor(Math.random() * Math.floor(999));
        const letter 
    }
}